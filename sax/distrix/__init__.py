from sax.distrix._t import pt, qt, rt, dt
from sax.distrix._normal import pnorm, qnorm, rnorm, dnorm
from sax.distrix._beta import pbeta, qbeta, rbeta, dbeta
from sax.distrix._gamma import pgamma, qgamma, rgamma, dgamma
from sax.distrix._uniform import runif, punif, qunif
from sax.distrix._chisq import pchisq, qchisq, rchisq, dchisq
from sax.distrix._pareto import ppareto, qpareto, rpareto, dpareto
from sax.distrix._f import pF, qF, dF
from sax.distrix._cauchy import pcauchy, qcauchy, dcauchy, rcauchy
from sax.distrix._exp import pexp, qexp, dexp, rexp
from sax.distrix._laplace import plaplace, qlaplace, dlaplace, rlaplace
from sax.distrix._weibull import pweibull, qweibull, dweibull, rweibull


